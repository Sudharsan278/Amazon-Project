spring.application.name=Amazonprj
#Updates if the table exists 
spring.jpa.hibernate.ddl-auto=update 
#For the above if create-drop is set for every time you run the code the old table is dropped and a new table is created so update is used
spring.datasource.url=jdbc:mysql://localhost:3306/amazon?createDatabaseIfNotExist=true&useSSL=true
spring.datasource.username=root
spring.datasource.password=2782004
server.port=8080
# Hibernate settings
#To display the sql queries in the console 
spring.jpa.show-sql=true 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Logging
logging.level.org.springframework=INFO
logging.level.com.your_package=DEBUG